# gh-maestro-coder / gh-maestro-senior-coder SKILL.md 追加候補

反省会で「コーダーSKILL.mdに追加できる」と分類されたが、即座には反映せずここに記録した項目。
まとめて見直すタイミングでSKILL.mdへの反映を検討する。

## PR説明・アーキテクチャ図と実装の自己整合性チェック

- 契機: PR #139（Issue #134）。PR説明・アーキテクチャ図では「Adapter層経由で配送する」としていたが、実装は当初Adapter層をバイパスしていた（Gemini指摘、その後修正）。
- 提案: 「PR説明文・アーキテクチャ図に書いた設計と、実装が実際に一致しているか確認する。未実装の部分があれば正直にそう書く」という自己整合性チェックの追加。

## バグ修正時は失敗パスを再現する回帰テストを同一コミットに含める

- 契機: PR #139（Issue #134）。pending再試行でAPI応答を破棄し本文が空のまま配送されるバグを修正したが、その具体的な失敗パス（lastBodyが空の状態から再試行する分岐）を再現する回帰テストが当初無かった（Review Manager指摘、severity: MAJOR）。
- 提案: 「レビュー指摘で発覚したバグを修正する際は、その具体的な失敗パス（バグが再現する条件）を再現する回帰テストを同一コミットに追加する」という原則の追加。既存の「新規関数にはテストを追加する」ルールとは別の、バグ修正specificな原則。

## 名前は実装より強い保証を主張しない（セマンティクス一致の命名）

- 契機: jintrick/gijiai Issue #365（同Issue #362のレビューで発覚）。共有ヘルパー`isExistingRecord`という名前は「レコードがDB上に実在するか」を検証しているように読めるが、実際は「IDの値が指定されているか」しか確認していなかった。値は指定されているが実在しないIDを「既存」と誤認し、保存処理が不整合を起こすバグにつながった。
- 提案: 実装作業をAIコーダーが担う以上、関数名・メソッド名はAIコーダーにとって仕様として読まれる。命名時に「この名前が主張する保証（存在確認・妥当性検証・冪等性など）を、実装は本当に満たしているか」を自己点検する原則を追加する。人間向けの簡潔さより実際の挙動を過不足なく言い表す正確さを優先し、そのために名前が長くなることを厭わない。一般的な省略語・曖昧な命名など、保証の強さの食い違いを伴わないスタイル上の指摘とは区別する。
