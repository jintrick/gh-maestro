# 用語集

このプロジェクトで使う言葉の定義。曖昧な言い換えを避けるため、ここにある語はこの意味でのみ使う。

## auto memory

Claude Code が自分のために書くメモ。人間の訂正や好みをもとに Claude 自身が書く。git リポジトリごとに `~/.claude/projects/<リポジトリ識別子>/memory/` へ置かれ、同じリポジトリの worktree はすべて同じディレクトリを共有する。人間が書く `AGENTS.md` の対になるもの。
_避ける_: 記憶、メモリ、ローカルメモ

## MEMORY.md

auto memory の目次ファイル。1件1行で、各メモへのリンクと短い要約を持つ。**先頭200行または25KB がセッション開始時に自動で読み込まれる。** 各メモの本文は目次とは別で、関連しそうなときにだけ読み込まれる。
_避ける_: インデックス、記憶の索引

出典: `docs/rag/claude-code/appendix/claude_code_glossary.md`
