---
paths:
  - ".claude/rules/**"
---

# ルールファイルの書き方

`paths:` を必ず指定する。省略すると毎セッション読み込まれる。

やってはならないこと・やらなければならないことだけを書く。コーダーが従うために必要な最小限に絞る。

事故の経緯、PR番号、設計判断の理由、スクリプトの挙動の解説は書かない。理由が必要な場合は ADR への参照1行に留める。
