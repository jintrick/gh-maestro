## 概要

<地の文（見出しで細分化しない）で、何が問題か・なぜ直すのかを簡潔に書く。問題が起きている場所を特定する固有名詞（ファイル名・パス・スクリプト名・識別子）は言い換えず、そのまま書く。書かないのは「どう直すか」であって、「何がどこで壊れているか」の特定ではない。実装手段の細部は書かない。>

## 振る舞い・制約・対象外

## 既決事項

## 未決事項

## 受け入れ条件

<具体的で、完了判定に追加の解釈を要しないように書くこと。対象を一般名詞へ言い換えず、ファイル名・パス・コマンド・行番号をそのまま書く。ワーカーはこの節だけを見て完了を判定するため、言い換えた時点で判定不能になる>


---

以下は Issue 本文に出力しない。オーケストレーター自身の起票前チェックとして使うこと。

## 起票前チェック

- 実装詳細（変更方針・作業分割・検証条件）をIssue本文に書いていない（これらはコーダーが計画フェーズでpin済みコメントとして作成する）
- 固有名詞を一般名詞へ言い換えていない（「AGENTS.md」を「規約文書」、「CLAUDE.md」を「副本」等と書き換えると、ワーカーには何一つ伝わらない）
