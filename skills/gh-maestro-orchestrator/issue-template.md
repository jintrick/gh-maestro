## 概要

<概要欄の読み手は人間である。この Issue が何の話で、なぜ着手するのかを、以降の節を読まずにここだけで掴めるようにする。それがこの節の目的であり、それ以外の目的はない。
地の文（見出しで細分化しない）で簡潔に書く。実装手段の細部や、コードを開かないと意味の取れない内部識別子は、人間の理解を助けないので書かない。対象を指すのに要る名前（AGENTS.md・CLAUDE.md など）は普通に書く。
この判断は概要欄に限る。以降の節には掛からない。>

## 振る舞い・制約・対象外

## 既決事項

## 未決事項

## 受け入れ条件

<具体的で、完了判定に追加の解釈を要しないように書くこと。対象を一般名詞へ言い換えず、ファイル名・パス・コマンド・行番号をそのまま書く。ワーカーはこの節だけを見て完了を判定するため、言い換えた時点で判定不能になる>


---

以下は Issue 本文に出力しない。オーケストレーター自身の起票前チェックとして使うこと。

## 起票前チェック

- 実装詳細（変更方針・作業分割・検証条件）をIssue本文に書いていない（これらはコーダーが計画フェーズでpin済みコメントとして作成する）
- 概要欄以外の節で固有名詞を一般名詞へ言い換えていない（「AGENTS.md」を「規約文書」、「CLAUDE.md」を「正本／副本」等と書き換えると、受け取ったワーカーには何一つ伝わらない）
